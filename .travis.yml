language: c
before_install:
    - sudo apt-get update
    - sudo apt-get install -y build-essential
before_script:
    - cd tepla
    - ./configure || { cat config.log && false; }
    - make
script: make -C tepla check
