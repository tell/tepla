language: c
compiler:
    - gcc
    - clang
before_install:
    - sudo apt-get update
    - sudo apt-get install -y build-essential
before_script:
    - export CC=gcc-4.8
    - cd tepla
    - ./configure || { cat config.log && false; }
    - make
script: make -C tepla check
